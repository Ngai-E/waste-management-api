import { Role } from '../enums/role.enum';

export const ADMIN_ROLES = [Role.ADMIN, Role.HYSACAM, Role.COUNCIL];
export const MANAGEMENT_ROLES = [Role.ADMIN, Role.HYSACAM, Role.COUNCIL];
export const ALL_ROLES = [Role.HOUSEHOLD, Role.AGENT, Role.ADMIN, Role.HYSACAM, Role.COUNCIL];
